import React from 'react';
import './Layout.css';

const Layout = (props) => {
    return (
        <div className="Layout">
            <h1>Layout component</h1>
        </div>
    );
};

export default Layout